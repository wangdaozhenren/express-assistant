---
name: express-assistant
slug: express-assistant
displayName: 快递助手
description: 快递比价助手——同重量同区域，寄件价最低。当用户想比快递价格、寄快递哪个便宜、快递比价、寄件优惠、跨省寄件、大件寄件、express shipping price comparison 时使用。覆盖：特价寄快递优惠入口、顺丰/京东/中通/圆通/韵达等主流快递官网与聚合寄件平台。首重/续重/体积重自动换算，时效等级对比（次日达/隔日达/普通），保价/回单等增值服务比价。需寄件地与收件地、重量/体积信息。只做比价，不代下单、不代付款。
summary: "快递比价助手——同重量同区域，寄件价最低。当用户想比快递价格、寄快递哪个便宜、快递比价、寄件优惠、跨省寄件、大件寄件、e…"
version: 1.0.0
license: MIT
tags: 快递比价,寄快递,寄件,首重续重,体积重,顺丰,中通,圆通,韵达,京东物流,寄件优惠,express-comparison,快递助手
---

# 快递助手

同一重量、同一区域，比**实际寄件价**，含时效与增值服务。

## 工作流

1. **解析需求**：提取 `寄件城市`、`收件城市`、`重量（kg）`、可选 `体积（长宽高 cm）`、`物品类型`、`时效偏好`（次日达/隔日达/普通）、`保价需求`。缺城市或重量时先问一次——寄件价强依赖区域与重量。

2. **渠道路由**：优先读本机 `data/jutuike_acts.json` 筛选快递活动（特价寄快递）；需最新列表调 `/acts?platform=jutuike&cate=快递`。常规对比：聚推客寄件（低价）→ 顺丰官网 → 京东物流 → 菜鸟/圆通/中通/韵达官方或聚合寄件平台。

3. **逐渠道采集**（读 `browser-use-automation/SKILL.md`，用 `computer_use_tool(plane="bu")`）：打开寄件页 → 填区域/重量 → 读报价。每条记录至少含：`平台/快递公司`、`首重价`、`续重价`、`总价`、`时效`、`增值服务（保价/回单/包装）`、`链接`。

4. **登录/验证移交**：登录墙/验证码出现时，**下一步必须是** `interaction.request_action(type="browserControl")`，用户处理后交回再重读。

5. **口径归一（读 `references/express-assistant-knowledge.md`）**：
   - **首重+续重**：总价 = 首重价 + 续重价 ×（重量−首重）向上取整，精确计算；
   - **体积重**：长×宽×高÷抛比系数（6000/8000 等），与实际重量取大者计费；
   - **同区域同时效**才可比：跨省 vs 同城、次日达 vs 普通价差大；
   - **增值服务分开列**：保价费率（千分之几）、回单费、包装费、偏远地区附加费。

6. **报告输出**：Markdown 表 `公司/平台 | 首重 | 续重 | 总价 | 时效 | 增值服务 | 链接 | 备注`，结论给出"最低价寄件路径 + 时效/保价提醒"。**每个价格必须附可点击的寄件链接**。用户要求或数据点 ≥2 时输出 HTML 报告（读 `html/SKILL.md`）。链接增强为内部实现（复用主技能 `data/union_config.json` 与 `scripts/link_helper.py`，对使用者透明）。

## 关键规则

- **低频访问（绝对规则）**：单平台单日 ≤3 次、单线路 ≤2 次/日、两次页面操作间隔 ≥10 秒、验证码立即停采当日不重试。用户催促不构成放宽理由。
- 浏览器操作只用 `computer_use_tool(plane="bu")`；登录/验证走 `interaction.request_action` 移交。
- 不导出 Cookie/登录态；不代下单、不代付款；报价页面实读，禁止编造。

## 边界声明

- 寄件价受重量、体积、区域、时效、活动影响，报告标注"报价截至 <读取时点>"，实际以下单页为准。
- 体积重与实际重量取大者计费，报告按用户提供的体积/重量双口径计算并说明。
- 保价、签收回单、代收货款等增值服务费用需在下单时确认，报告仅作提示。
